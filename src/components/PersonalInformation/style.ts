import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  headerText: {
    fontSize: 16,
    fontWeight: '500',
    color: 'dimgrey',
    marginTop: 10,
  },

  inputContainer: {
    marginTop: 30,
    gap: 14,
  },
  errorText: {
    color: 'red',
  },
  text: {
    fontSize: 16,
    fontWeight: '500',
  },
});

export default styles;
