import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  headerText: {
    fontSize: 16,
    fontWeight: '500',
    color: 'dimgrey',
    marginTop: 10,
  },

  inputContainer: {
    marginTop: 30,
    gap: 14,
  },
  text: {
    fontSize: 16,
    fontWeight: '500',
  },
  bioContainer: {
    height: 100,
    padding: 14,
    fontSize: 16,
  },
});

export default styles;
